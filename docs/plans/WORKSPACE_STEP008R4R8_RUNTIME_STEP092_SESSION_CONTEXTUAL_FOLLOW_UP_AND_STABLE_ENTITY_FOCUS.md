# WORKSPACE STEP008R4R8 / Runtime STEP092

```text
Current Workspace: WORKSPACE_STEP008R4R8_RUNTIME_STEP092_SESSION_CONTEXTUAL_FOLLOW_UP_AND_STABLE_ENTITY_FOCUS
Workspace Version: 0.8.4-r8
Current Runtime: STEP092_SESSION_CONTEXTUAL_FOLLOW_UP_AND_STABLE_ENTITY_FOCUS
Runtime Version: 2.76.0
State: IMPLEMENTED_STATIC_VALIDATION_ONLY_TEST_EXECUTION_DEFERRED_BY_USER_MINIO_PENDING
Promotion: NOT_READY
```

## Objective

Close the smallest Agent-core gap found in the current code: the router could recognize standalone Organization Context short expressions, but it had no Product-owned stable reference to the entity/candidate set established by the prior Tool result.

## Implemented scope

1. Add canonical Session Context Focus domain records.
2. Persist focus in SQLite/PostgreSQL Session metadata, separate from encrypted SDK history.
3. Derive focus only from normalized allowlisted Organization MCP Tool output.
4. Resolve bounded Korean deictic/ellipsis follow-up from stable candidate evidence.
5. Preserve ambiguity and allow only bounded ordinal/qualifier disambiguation.
6. Add GET as a Product-owned named Tool operation for stable-ID follow-up.
7. Fence returned GET evidence against immutable entity type/ID.
8. Commit focus only with successful Session Turn release; accept it only for the most recent committed Turn; clear with Session clear.
9. Make one preflight use one Session-focus snapshot and one route decision; align Admin/Service immutable routing-context wrapping.
10. Keep the public ambiguous candidate list identical to the durable 20-candidate focus bound.
11. Prepare STEP092 deterministic regression/acceptance/partition launchers without executing them. The retained Workspace Live runner remains a parent short-expression regression and is not sufficient to claim STEP092 contextual-live acceptance.
12. Preserve STEP091D MinIO-pending boundary and historical STEP091B3R1 PostgreSQL evidence.

## Non-goals

Relation traversal, cross-domain focus, long-term memory, distributed Session history, write actions and Automation are not part of this Step.

## Acceptance state

Implementation and static validation only. All tests are deferred by user direction until MinIO is prepared. Promotion remains NOT_READY.
