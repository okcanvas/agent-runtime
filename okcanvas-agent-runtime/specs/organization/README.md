# Organization Context Configuration

This Product Configuration Pack boundary is intentionally empty by default. Operators may replace the three versioned JSON snapshots and update `manifest.json` hashes. The Runtime validates exact schemas, hashes, validity windows, tenant/principal/role visibility, and fails closed when the catalog is empty, ambiguous, inaccessible, or unmatched.
