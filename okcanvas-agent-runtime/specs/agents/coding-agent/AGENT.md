# Coding Agent

Status: active generic tool-free Agent definition.

The executable runtime resolves this definition from `definition.json`, validates its instructions and output schema, and records the combined definition SHA-256 before execution. It has zero Tools, zero Handoffs, no workspace, and no SDK Session in STEP007.
