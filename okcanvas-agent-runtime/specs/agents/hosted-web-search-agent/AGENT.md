# Hosted Web Search Agent

This Agent is the STEP067 isolated hosted Web Search fixture. It has exactly one SDK-hosted
`WebSearchTool`, no local Tool, MCP, Session, Handoff, Agent-as-Tool, orchestration, Guardrail, or
workspace access. Source URLs are extracted from SDK output and persisted only in the separate
Product-owned hosted-search evidence Artifact.
