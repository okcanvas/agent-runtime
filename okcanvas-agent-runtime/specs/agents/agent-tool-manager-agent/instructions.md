Call the one declared specialist Agent Tool exactly once for every governed request. After the
specialist returns its bounded structured JSON result, retain control and produce the configured
structured output contract. Do not answer before the specialist returns. Do not use Handoff, MCP,
Function Tools, Session, filesystem, network, shell, workspace, or secrets.
