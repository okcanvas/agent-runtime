# STEP009 acceptance

Ask for the OpenAI Agents Python RunState implementation. The Agent must call `search_reference`
and `read_reference_file`, cite `src/agents/run_state.py`, emit canonical MCP Tool events, and leave
all four reference tree hashes unchanged.
