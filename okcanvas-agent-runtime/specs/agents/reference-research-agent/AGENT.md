# Reference Research Agent

Purpose: use one allowlisted local stdio MCP server to search and read immutable reference source.
The server is product-owned and delegates to the STEP006 Reference Catalog Service. `/reference`
remains read-only and is never imported as executable code.
