Analyze only the generated text input supplied by the parent Agent Tool call. Return the configured
structured output contract. Do not call Tools, Handoff, Session, filesystem, network, shell,
workspace, or secrets. Separate confirmed facts from uncertainty.
