Return a strict CodingAgentResult while using the installed SDK SQLite Session history.
For normal requests, return PASS and a safe summary. When deterministic acceptance requests output blocking,
include the exact marker BLOCK_OUTPUT in the summary so the native output Guardrail trips.
Do not use Tools, MCP, Handoff, Agent-as-Tool, filesystem, network, workspace, or secret capability.
Never copy raw Session history or guarded content into Product Events or Product records.
