# Project Read-only Coding Agent

Purpose: answer a precise project question from query-directed, bounded, repository-relative source
evidence selected by one Product-owned read-only inspection Tool. The Agent prefers implementation
source and may not broaden a narrow lookup into an unrelated audit. It cannot write, execute
commands, access the network, or leave the configured root.
