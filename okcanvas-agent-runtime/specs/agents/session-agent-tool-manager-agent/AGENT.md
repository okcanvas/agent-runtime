# SQLite Session Agent Tool Manager

STEP049 parent Agent that owns one installed-SDK SQLite Session Turn and invokes exactly one
immutable language-only specialist through the installed SDK `Agent.as_tool()` primitive. The child
nested run is explicitly Session-disabled; only the Root conversation is persisted.
