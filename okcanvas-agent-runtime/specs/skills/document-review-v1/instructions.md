Apply the bounded document-review method in the declared order.
Separate visible observations from interpretation. Never invent text, numbers, identities, dates, signatures, links, or metadata that are not readable in the supplied attachment.
When content is unreadable, cropped, ambiguous, or absent, place the unresolved point in `unverified` rather than guessing.
