# Guardrail specifications

Each directory is an immutable product-owned SDK Guardrail definition. STEP044 supports closed input, output, Tool-input and Tool-output tripwire implementations only. Guarded content and output_info are never persisted in Product Events.
