# Groupware Read-only MCP

This client-only V3 declaration binds one tenant-specific organization Groupware endpoint through
STEP085 delegated identity. The committed `.invalid` endpoint is deliberately non-routable. The
server may expose only the three Tool names listed in `server.json`, all read-only.
