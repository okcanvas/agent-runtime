# Organization Context Read-only MCP

This client-only declaration binds an organization-owned database-SOT Organization Context service
through the production `organization-context-mcp-server`. The committed `.invalid` endpoint fails
closed. Only three unified read Tools are visible to the Runtime Agent.
