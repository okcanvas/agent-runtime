# Architecture governance

The canonical ratified architecture constitution is packaged under
`specs/architecture/constitution/`. Runtime identity is exposed through
`okcanvas_agent_runtime.governance.resolve_architecture_constitution()`.

No source-layout migration may begin before the current Windows live acceptance
closes and the applicable constitution clause IDs and mandatory gates are listed
in the STEP compliance record.
