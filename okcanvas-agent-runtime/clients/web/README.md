# agent-web

Planned browser client for the multi-user OKCanvas Agent Runtime.

The future application consumes only `/v1/service/**` JSON and persisted SSE. It does not receive the
local administrator key, Run-submitter key, provider API key, filesystem paths, SQLite paths, native
SDK stream, or Runtime implementation modules.
