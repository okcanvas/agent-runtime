import { type RuntimeConfig } from "./config.js";
import type { CliOptions } from "./types.js";
export declare class PersistentAgentCli {
    private readonly client;
    private readonly config;
    private readonly options;
    private readonly input;
    private agents;
    private activeAgent;
    private activeSession;
    private model;
    private lastOutcome;
    private requestCount;
    private debugEnabled;
    private runtimeHealth;
    private readonly localConversation;
    constructor(config: RuntimeConfig, options: CliOptions);
    run(): Promise<number>;
    private sessionEnabled;
    private prompt;
    private selectInitialAgent;
    private activateSession;
    private validateSessionForActiveAgent;
    private printBanner;
    private handleCommand;
    private changeAgent;
    private createNewSession;
    private showCurrentSession;
    private sessionsForActiveAgent;
    private showSessions;
    private resumeSession;
    private clearCurrentSession;
    private showLocalHistory;
    private printStatus;
    private handleDebugCommand;
    private evaluateLastRun;
    private executionObserver;
    private executeRequest;
}
