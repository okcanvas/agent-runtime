# Codex controlled-write acceptance fixture

The known defect is intentionally retained. `calculate_total` ignores `quantity`.
The write Agent must discover and minimally repair the implementation without changing the test.
