# Codex read-only acceptance fixture

The known defect is intentionally retained. `calculate_total` ignores `quantity`.
The read-only Agent must discover the relevant implementation and test without receiving file paths in the task prompt.
