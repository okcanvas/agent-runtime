# Workspace Reference

This directory records accepted parent artifact identities and byte-level file manifests used to prove
that WORKSPACE STEP001 is a structural reassembly rather than a silent product modification.
